import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.neotokyo.game",
	appName: "NeoTokyo Rival Academies",
	webDir: "dist",
};

export default config;
